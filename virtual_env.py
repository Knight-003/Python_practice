"""    we can create a virtual environment in which all the python dependencies can be install to avoid phasing out of a function 
         the virtual environment's python is total indipendent of python on main computer
         we can create a text file that will have all the dependencies name
         
         
         we can install specific module by giving it a specific version 
         we can also install through text file that have all the name and versions of dependencies 
         
                  pip freeze > requirements.txt
                  set-executionpolicy remotesigned
                  pip install -r .\requirements.txt
                  virtualenv --system-site-packages nikh2(this will create virtual environments same as on main computer)"""
         

